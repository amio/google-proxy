{
  "private": true,
  "scripts": {
    "start": "node index.js",
    "deploy": "now rm google-proxy --safe -y && now && now alias"
  },
  "dependencies": {
    "http-proxy": "^1.17.0",
    "http-proxy-middleware": "^0.19.1",
    "serve-marked": "^2.0.0"
  },
  "engines": {
    "node": "^10"
  }
}
